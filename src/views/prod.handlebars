<div class="container p-4">
    <div class="row">
        {{#each productos }}
        <div class="col-sm-4">
            <div class="card">
                <div class="card-body">
                    <img src="{{path}}" class="card-img-top">
                    <div class="card-body">
                        <h5 class="card-title">
                            {{nombre}}
                        </h5>
                    </div>
                </div>
            </div>
        </div>
        {{/each}}
    </div>
</div>